## 📦 Installation Steps

1.  **Clone the repository** (if using git):
    ```bash
    git clone <your-repo-url>
    cd <your-project-folder>
    ```

2.  **Install Dependencies**:
    Run the following command to install all required packages (React, GSAP, Framer Motion, Swiper, etc.):
    ```bash
    npm install
    ```

    *If you are building this from scratch, here is the command to install the specific libraries used in this project:*
    ```bash
    npm install react-router-dom gsap framer-motion swiper lucide-react @studio-freight/lenis
    ```

3.  **Install Tailwind CSS** (If not already configured):
    ```bash
    npm install -D tailwindcss postcss autoprefixer
    npx tailwindcss init -p
    ```

## 🏃‍♂️ How to Run

### Development Mode
To start the local development server:

```bash
npm run dev